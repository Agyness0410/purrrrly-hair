# Environment Configuration Example
PORT=3000
NODE_ENV=production

# Optional: Database URL for PostgreSQL (if migrating from SQLite)
# DATABASE_URL=postgresql://username:password@host:port/database

# Optional: Admin access key for additional security
# ADMIN_ACCESS_KEY=your-secret-admin-key